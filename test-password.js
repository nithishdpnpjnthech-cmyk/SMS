// Quick password test\nconst bcrypt = require('bcrypt');\n\nasync function testPassword() {\n  const studentName = 'meenakshi';\n  const expectedPassword = studentName.replace(/[^a-zA-Z]/g, '').substring(0, 5).toLowerCase();\n  const storedHash = '$2b$10$Ppbp9mk2hjvIa1Qc62SoCuRs19yynaSJobBp/U3s22v0VUR/iOrZe';\n  \n  console.log('Student name:', studentName);\n  console.log('Expected password:', expectedPassword);\n  console.log('Stored hash:', storedHash);\n  \n  // Test bcrypt comparison\n  const isValid = await bcrypt.compare(expectedPassword, storedHash);\n  console.log('Password valid:', isValid);\n  \n  // Test with different variations\n  const variations = ['meena', 'Meena', 'MEENA', 'meenakshi', 'meenakshi89'];\n  \n  for (const variation of variations) {\n    const result = await bcrypt.compare(variation, storedHash);\n    console.log(`Testing '${variation}':`, result);\n  }\n}\n\ntestPassword().catch(console.error);